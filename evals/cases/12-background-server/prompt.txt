server.py is a long-running HTTP server (port 18765). Start it in the background with the bash tool,
wait until it responds, fetch http://127.0.0.1:18765/health with curl, save the exact JSON body to
health.json, then stop the server so nothing is left running on the port.
